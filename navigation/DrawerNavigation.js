import React from 'react';
import { Text } from 'react-native';
import { DrawerNavigator } from 'react-navigation';

const routeConfig = {

};

const DrawerNavigation = DrawerNavigator(routeConfig);

export default DrawerNavigation;